using UnityEngine;
using System.Collections;

/// <summary>
/// This is the class that will be responsible for managing an interfacing game.
/// </summary>
public class InterfacingGame : MonoBehaviour {

	/// <summary>
	/// This is the message that will be sent when the game is being interacted with.
	/// </summary>
	public const string MSG_INTERACTED = "MSG_INTERACTED";

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	enum State {
	}
}
